# Ahmed Sayed Mansour — Portfolio (Vite + React + Tailwind)

This is a ready-to-deploy portfolio built with Vite + React + Tailwind CSS.
It includes your photo and CV (located in /public).

## How to run locally

1. Install dependencies:
```bash
npm install
```

2. Run dev server:
```bash
npm run dev
```

3. Build for production:
```bash
npm run build
npm run preview
```

## Deploy to Vercel (recommended)
1. Create a GitHub repository (e.g. `ahmed-portfolio`) and push all files.
2. Go to https://vercel.com, sign in with GitHub, and click "New Project".
3. Import your repository and accept the defaults (Vite is auto-detected).
4. Click "Deploy". Your site will be live on a vercel.app domain.

Public assets:
- CV: /CV.pdf
- Photo: /assets/IMG_2267.jpg

If you want me to push this repo into your GitHub (I cannot push directly), I can give you the exact git commands to run locally.
